services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    image: flutter-lms-backend:latest

    container_name: flutter-lms-api

    restart: unless-stopped

    env_file:
      - .env

    ports:
      - "${PORT:-5000}:${PORT:-5000}"